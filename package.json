{
  "name": "aws-weather-app",
  "version": "1.0.0",
  "description": "A aws lambda service that sends daily sms reminders with weather information.",
  "main": "weather-lambda.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Huang",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.600.0",
    "axios": "^0.19.1",
    "dotenv": "^8.2.0"
  }
}
